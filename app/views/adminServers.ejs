<%- include('./partials/header'); -%>
<main class="admin__main">
  <h1 class="h1-admin-page">Gestion des serveurs - Administrateurs</h1>
  <%- include('./partials/adminNavBar'); -%>
  
  <div class="admin__container-list">
    <% locals.servers.forEach(server => { %>
      <article class="article__server">
        <div class="article__title-server"><h2><%= server.name %></h2></div>
        <img class="logo__server" src="/images/<%= server.img %>" alt="image du serveur">
        <a href="/admin/servers/update/<%= server.id %>"><input class="input-feature-admin" type="button" value="Modifier"></a>
        <a href="/admin/servers/delete/<%= server.id %>"><input class="input-feature-admin" type="button" value="Supprimer"></a>
      </article>
    <% }) %>
    <article class="article__server">
      <button id="addButton" class="circular-button">+</button>
      <form action="/admin/servers-add" method="POST" id="addServerForm" class="addServerForm hidden" enctype="multipart/form-data">
        <label for="serverName">Nom du serveur</label>
        <input type="text" id="serverName" name="serverName" required>

        <label for="game1"> 
          <img src="/images/dofusretro.jpeg" alt="logo dofus rétro">
        </label>
        <input type="radio" id="game1" name="game" value="dofus retro" checked required>

        <label for="game2">
          <img src="/images/dofus.jpeg" alt="logo dofus 2">
        </label>
        <input type="radio" id="game2" name="game" value="dofus 2" required>
        
        <label for="serverImage">Image</label>
        <input type="file" id="serverImage" name="serverImage" accept="image/*" required>

        <input class="input-feature-admin" type="submit" value="Ajouter">
        <input class="input-feature-admin" type="button" id="cancelAdd" value="Annuler">
      </form>
    </article>
  </div>
</main>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const addButton = document.getElementById('addButton');
    const addServerForm = document.getElementById('addServerForm');
    const cancelButton = document.getElementById('cancelAdd');

    addButton.addEventListener('click', function() {
      addServerForm.classList.toggle('hidden');
      addButton.classList.toggle('hidden');
    });

    cancelButton.addEventListener('click', function() {
      addServerForm.classList.toggle('hidden');
      addButton.classList.toggle('hidden');
    });
  });
</script>
<%- include('./partials/footer'); -%>